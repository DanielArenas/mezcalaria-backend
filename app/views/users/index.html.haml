%h2 Usuarios

%hr

%table.table
  %thead
    %tr
      %th Nombre
      %th email
      %th fecha de registro
      %th Language
  %tbody
    - @users.each do |user|
      %tr
        %td= user.name
        %td= user.email
        %td= user.created_at
        %td= user.get_language 

%br

