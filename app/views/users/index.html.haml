%h2 
  Usuarios
  %a{:class => "btn btn-success", :href => "/users.csv", :target => "_blank"} Descargar

%hr

%table.table
  %thead
    %tr
      %th Nombre
      %th email
      %th fecha de registro
      %th os
      %th Language
  %tbody
    - @users.each do |user|
      %tr
        %td= user.name
        %td= user.email
        %td= user.created_at
        %td= user.type_device
        %td= user.get_language 

%br

