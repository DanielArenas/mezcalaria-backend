<%- headers = ['Nombre', 'Email', 'Idioma', 'OS', 'Token'] -%>
<%= CSV.generate_line headers %>
<%- @users.each do |user| -%>
<%= CSV.generate_line([ 
			user.name,
			user.email,
			user.type_device,
			user.device_id,
		])
	-%>
<%- end -%>