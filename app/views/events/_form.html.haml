= simple_form_for(@event) do |f|
  = f.error_notification

  = f.input :name
  = f.input :place
  = f.input :date_event
  = f.input :baner
  = f.input :is_free
  = f.select :language, [ ['Español', 1], ['English', 2] ]
  
  .form-actions.pull-right
    = f.button :submit, 'Guardar Event'
    = link_to events_path, class: 'btn btn-danger' do
      = icon 'times', 'Cancelar'
