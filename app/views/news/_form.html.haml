= simple_form_for(@news) do |f|
  = f.error_notification

  = f.input :tittle
  = f.input :description
  = f.input :video
  = f.input :photo
  = f.select :language, [ ['Español', 1], ['English', 2] ]

  .form-actions.pull-right
    = f.button :submit, 'Guardar News'
    = link_to news_index_path, class: 'btn btn-danger' do
      = icon 'times', 'Cancelar'
