%p#notice= notice

%h2 Noticias

%hr

%table.table
  %thead
    %tr
      %th Tittle
      %th Img
      %th Language
      %th Acciones
  %tbody
    - @news.each do |news|
      %tr
        %td= news.tittle
        %td= image_tag(news.photo.thumb.url) 
        %td= news.get_language
        %td
          .btn-group
            = link_to 'Mostrar', news, class: 'btn btn-sm btn-default'
            = link_to 'Editar', edit_news_path(news), class: 'btn btn-sm btn-primary'
            = link_to 'Eliminar', news, method: :delete, data: { confirm: '¿Estás seguro?'}, class: 'btn btn-sm btn-danger'

%br

.pull-right
  = link_to new_news_path, class: 'btn btn-primary btn-lg' do
    = icon 'plus', "Nuevo Noticias"
